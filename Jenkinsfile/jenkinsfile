@Library('jenkins-shared-lib-2025') _

pipeline {
    agent any


    stages {

        stage('Git Checkout'){
                       when { expression {  params.action == 'create' } }
               steps{
               gitCheckout(
                   branch: "main",
                   url: "https://github.com/mhulic/jenkins-javaapp-2025.git"
               )
           	}
        }

        stage('Unit Test Maven'){
                       when { expression {  params.action == 'create' } }
               steps{
				   mvnTest()
           	}
        }
    }
}